import { UiColorGenPipe } from './ui-color-gen.pipe';

describe('UiColorGenPipe', () => {
  it('create an instance', () => {
    const pipe = new UiColorGenPipe();
    expect(pipe).toBeTruthy();
  });
});
