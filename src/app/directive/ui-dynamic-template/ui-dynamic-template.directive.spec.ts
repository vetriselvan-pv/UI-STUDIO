import { UiDynamicTemplateDirective } from './ui-dynamic-template.directive';

describe('UiDynamicTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new UiDynamicTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
